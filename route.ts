export const publicRoutes = [
    "/",
    "/new-verification",
    "/reset-password",
]

export const authRoutes = [
    "/"
]

export const adminRoutes = [
    "/projects",
    "/users",
    "/tasks"
]

export const apiAuthPrefix = "/api/auth"

export const DEFAULT_LOGIN_REDIRECT = "/category"
export const ONBOARDING_LOGIN_REDIRECT = "/onboarding"